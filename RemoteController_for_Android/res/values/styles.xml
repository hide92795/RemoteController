<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

     <!-- Preference category theme. -->
    <style name="PreferenceCategory">
        <item name="android:layout">@layout/pref_title_layout</item>
    </style>

    <!-- Preference theme. -->
    <style name="PreferenceTheme" parent="Theme.AppCompat.Light">
        <item name="android:preferenceCategoryStyle">@style/PreferenceCategory</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme"></style>

    <!-- Application theme with background. -->
    <style name="AppThemeWithBackground" parent="AppBaseTheme">
        <item name="android:background">@color/background</item>
    </style>

</resources>